import styled from "@emotion/styled";
import { MemoHeader } from "components/domain/Memo/styles";

export const EditHeader = styled(MemoHeader)``;

export const EditBody = styled.div`
  margin-top: 15px;
`;
