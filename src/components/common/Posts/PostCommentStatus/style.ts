import styled from "@emotion/styled";

export const CommentStatusWrap = styled.div`
  display: flex;
  align-items: center;

  .likes-icon {
    display: flex;
    padding: 0 3px;
    font-size: 20px;
    cursor: pointer;
  }
  .btn-link {
    cursor: pointer;
  }
  .count {
    padding-left: 3px;
  }
`;
